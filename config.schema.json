{
  "pluginAlias": "HCPBridgeMqttHomebridgePlugin",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "GarageDoor"
      },
	  "displayName": {
	    "type": "string",
	    "title": "Accessory Display Name",
	    "placeholder": "Garage Door",
	    "description": "Name shown in the Home app"
	  },
      "mqttHost": {
        "type": "string",
        "title": "URL",
        "description": "MQTT broker URL default: mqtt://localhost:1883",
        "placeholder": "mqtt://localhost:1883"
      },
      "mqttUsername": {
        "type": "string",
        "required": false,
        "title": "Username",
        "description": "MQTT Username (optional)"
      },
      "mqttPassword": {
        "type": "string",
        "required": false,
        "title": "Password",
        "description": "MQTT Password (optional)"
      },
      "mqttClientID": {
        "type": "string",
        "title": "ClientID",
        "required": false,
        "description": "MQTT Client ID (optional, default: HomeBridge)"
      },
      "targetTopic": {
        "type": "string",
        "title": "TARGET door state topic",
        "placeholder": "hormann/hcpbridge/command/door",
        "description": "MQTT Topic to indicate the desired target door state."
      },
      "currentTopic": {
        "type": "string",
        "title": "CURRENT door state topic",
        "placeholder": "hormann/hcpbridge/state",
        "description": "MQTT Topic for the current door state"
      },
      "mqttOpenMessage": {
        "type": "string",
        "title": "OPEN message",
        "placeholder": "open",
        "description": "MQTT message payload for the target door state 'open'"
      },
      "mqttCloseMessage": {
        "type": "string",
        "title": "CLOSE message",
        "placeholder": "close",
        "description": "MQTT message payload for the target door state 'close'"
      },
      "mqttStopMessage": {
        "type": "string",
        "title": "STOP message",
        "placeholder": "stop",
        "description": "MQTT message payload for the target door state 'close'"
      }
    }
  }
}